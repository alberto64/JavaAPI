public class CesarMethod {
    /*
     * This program is will encrypt a text using the Cesar Method.
     * */
	
    public static String encodingtext(String text, int key)
    {
        String newstr = "";
        int size = text.length();
        for(int i = 0; i< size;i++)
        {
            //Write the code to compose a string and do the math to encode the user's input
        }
        return newstr;
    }

    public static String decodingtext(String text, int key)
    {
        String newstr = "";
        int size = text.length();
        for(int i = 0; i< size;i++)
        {
        	//Write the code to compose a string and do the math to decode the user's input
        }
        return newstr;
    }
}